# 10. Аспекты

Настроил rate limit на количество обращений к апи во временной промежуток.

Используется аннотация `RateLimit`, частота запросов задаётся в конфигурации, `RateLimitAspect` содержит логику
обработки аспекта, а `RateLimitStorage` считает количество запросов